class ConexionFLS{

    constructor(channelId, guildId, adapterCreator, desconexion){
        this.channelId == channelId;
        this.guildId == guildId;
        this.adapterCreator == adapterCreator;
        this.selfDeaf;
        this.desconexion == desconexion;
    }
}