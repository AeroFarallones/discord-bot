const { SlashCommandBuilder } = require('discord.js');

module.exports = {
	data: new SlashCommandBuilder()
		.setName('verify')
		.setDescription('Para poder disfrutar de nuestro servidor, verificate!')
        .setName('verify'),
        async execute() {

        },
};